extends layout

block content
  .error-page
    .container
      .row
        .col-12
          h1= error.status
          p= error.message
          if (error.status === 404)
            p The page you're looking for doesn't exist.
          else
            p Something went wrong on our end.
